# Активный контекст (Active Context)

> Обновлять в конце каждой сессии.

## Где мы сейчас (2026-09-26)

**Апгрейд «Лазер наготове» не теряется на старте уровня** (исправлено, 26.09.2026):

- Причина: запуск уровня взводил лазер апгрейда, но оставлял `laserArmedUntil = 0`;
  `updateLaser` сбрасывал его на первом кадре после стартовой блокировки.
- При старте кампанийного/бесконечного боя лазер апгрейда теперь получает
  бесконечный срок взвода до первого выстрела; обычный бонусный лазер по-прежнему
  ограничен четырьмя секундами.
- Регрессионный тест пережидает стартовую паузу и проверяет запуск лазерного залпа.
- Проверки: lint без ошибок (9 существующих предупреждений), typecheck успешен,
  `npm run test` — 30 файлов / 259 тестов зелёные.

**Медуза достижима шаром на смартфоне** (исправлено, 26.09.2026):

- Причина: на портретных экранах шар не поднимается выше `blockTop`, а медуза
  клампилась по центру к `h*0.12`; при большой HUD-зоне купол оказывался полностью
  выше доступной области. В `BossHost` добавлен `blockTop`, а `stepJellyfish`
  удерживает центр купола не выше `blockTop + bo.r`.
- Добавлена функция `jellyBodyMinY` и регрессионный тест; отростки продолжают
  пересчитываться относительно достижимого купола.
- Проверки: `lint:check` и `typecheck` зелёные (9 старых предупреждений lint),
  `npm run test` — 30 файлов / 258 тестов зелёные.

**Окантовка HP босса-медузы показывает урон** (сделано, 26.09.2026):

- В `render/bossJellyDome.ts` базовая окантовка купола теперь получает тёмный оттенок
  цвета текущей фазы медузы (`jellyDomeContourColor`), а красный участок контура
  рассчитывается по доле полученного урона (`1 - hp/maxHp`), а не по оставшемуся HP.
- Общая геометрия купола, симметрия от макушки и рисование внутри трансформа пульса
  не изменены; обновлены регрессионные тесты цвета и полного урона.
- Проверки: `lint:check` и `typecheck` зелёные (9 старых предупреждений lint),
  `npm run test` — 30 файлов / 254 теста зелёные.

**Красная заливка тела медузы в злой фазе** (сделано, 26.09.2026):

- В `render/bossJellyDome.ts` добавлен clipped overlay по общему пути купола:
  в злой фазе красный светлый градиент заполняет тело сверху вниз пропорционально
  полученному урону (`1 - hp/maxHp`), не выходя за контур и бахрому.
- `render/boss.ts` включает overlay только при `hp < maxHp * 0.5`; окантовка урона
  остаётся поверх тела. Добавлены тесты доли урона, клипованной заливки и фазового
  переключения.
- После изменения: `npm run test` — 30 файлов / 256 тестов зелёные.

**Полоска HP по контуру купола босса-медузы** (сделано, 25.09.2026):

- Новый модуль `render/bossJellyDome.ts` — единственный источник геометрии купола:
  `traceJellyDome` (заливка настоящими кривыми), `jellyDomeContour` (полилиния той же
  кривой — Canvas не умеет измерять длину пути), `jellyHpContourSlice` (обрезок длиной
  frac·L, симметричный от макушки), `drawJellyDomeHp` (тёмная подложка 4 px + заливка
  2.5 px, цвет по остатку HP — палитра кольца боссов).
- Бахрома купола выровнена: было 4 лопасти только на правой половине низа (левая —
  прямой `closePath`, баг с первого коммита, проверен GDI+-превью), стало 8 лопастей
  через всю ширину.
- Полоска вызывается внутри трансформа купола (`translate` + `scale` пульса), поэтому
  изгибается вместе с телом и повторяет волну бахромы; рисуется после вспышки урона,
  глаза/рот — поверх неё.
- `frameInvariant.helpers.ts`: в `PaintEvent` добавлены `strokeStyle`/`lineWidth`.
- Тесты `render/bossJellyDome.test.ts` (9): ломанная идёт по путям купола (<0.5 px),
  бахрома на всю ширину, срез пропорционален HP / симметричен от макушки / лежит на
  рёбрах контура, полоска рисуется под масштабом пульса при `globalAlpha === 1`.
  Пойман баг: при frac = 1 срез не замыкал контур (нет замыкающего ребра).
  **30 файлов / 254 теста — зелёные**; typecheck/lint — чисто (9 старых предупреждений);
  файлы в лимитах (boss.ts 390, bossJellyDome.ts 201).
- Замечание: `campaignMap.test.ts` «структурные инварианты» (~4–5 с) при полной
  нагрузке иногда упирается в дефолтный таймаут 5 с — предсуществующая флаки
  (5/5 зелёных прогонов подряд после того, как нагрузка упала).
- Нюансы сессии вынесены в файлы, читаемые с самого начала задачи: `.clinerules`
  (Terminal — явный Git Bash и ловушки PowerShell; Pre-commit — многострочные
  сообщения через `git commit -F`; Testing — флаки `campaignMap`; Code Style —
  рендер лежит в `src/game/render/` и правило «геометрия одной фигуры — из одних
  формул»), `techContext.md` (путь Git Bash, коммиты через -F, записывающий ctx
  для тестов рендера, GDI+-превью для проверки Canvas-геометрии),
  `systemPatterns.md` (паттерн «общий контур: fill + полоска HP», recording-ctx).
  Код этой задачи запушен коммитом `95ea018` в `origin/beta`.

## Где мы раньше (2026-09-23)

**Тема 1 — шесть новых типов блоков (ROADMAP §6)** (сделано):

- **Данные** `blockKinds.ts`: интерфейс `BlockSpecial` (поле `Block.sp`) + константы
  баланса (PULSE_AMPLITUDE, SPRING/COTTON_SPEED_MULT и времена, PORTAL_CD, SPIN_KICK/MAX).
  `Ball` получил таймеры `springT`/`cottonT`.
- **Маршрутизатор траекторий** `game/blockMotion.ts` (`stepBlock`): старые
  sway/bob перевезены из `updateStep` + новые маршруты дрейфа (горизонталь /
  вертикаль / окружность с эллипсом 0.6), пульсация размера (хитбокс следует
  за визуалом — collideBlocks читает rx/ry), интеграция вращения с трением
  (exp(-1.1·dt), отсечка <0.02), тик кулдауна порталов.
- **Эффекты удара** `physics/special.ts` (`onBallHitSpecial` из `collideBlocks`
  до отражения): пружина/вата ставят таймеры на шаре (звук+бёрст), повторный
  удар не перезапускает активный таймер; крутящийся получает угловой импульс
  от стороны и глубины удара (кламп SPIN_MAX); телепорт поглощает контакт:
  переброс к живому партнёру по вектору скорости, кулдаун на всю пару,
  сброс хвоста шара. Множители применяются в `Physics.updateBall` через
  `specialSpeedMult`.
- **Расстановка** `game/blockSpawn.ts` (`decorateBlocks` из `buildFromSpec`,
  кроме боссовых арен): детерминированно из seed (кампания — seed уровня,
  бесконечный — daySeed-волна); интенсивность `specialIntensity` = 0 до 3-го
  уровня, растёт до 0.55; каждый блок — максимум один спецтип; порталы — пары
  с общим id и hp 2 (с 4-го уровня, вторая пара с 9-го); исключены минибоссы,
  щупальца, орбитальные и бомбы; дрейф отключает сеточное sway; крутящийся —
  только вытянутые некруглые.
- **Рендер** `render/blockKinds.ts`: кастомные тела пружины (сжатие при ударе
  через flash), ваты (пушистое облако), портала (тёмное ядро + вращающееся
  аккреционное кольцо, тускнеет на кулдауне) — градиенты через gradCache;
  метки поверх стандартного тела: кольцо пульсации, штрихи маршрута, стрелки
  вращения.
- **Звук** `audio.ts`: `spring()` (бо-инг вверх), `thud()` (глухой шлепок),
  `warp()` (воронка-свип).
- Тесты: `blockMotion.test.ts` (9), `special.test.ts` (11), `blockSpawn.test.ts`
  (8): маршруты/пульсация/вращение/кулдаун, семантика ударов, пары порталов,
  исключения, детерминизм. **24 файла, 214 тестов — зелёные**; typecheck/lint/
  build — чисто; все файлы в лимитах (types.ts 298, audio.ts 448).

**Тема 2 — осмысление карт и коопа (сделано, 24.09.2026, без реализации):**

- **Пользовательские карты (§13)** — делать поэтапно: этап A «код карты»
  (детерминированный формат поверх `levels`/`PatternSpec`, спецблоки §6 через
  сид-декоратор; импорт/экспорт строкой, «мои карты» в localStorage, тест-прогон
  через готовый `buildFromSpec`) — следующий большой этап; этап B (каталог
  Supabase: таблица `maps` + Edge Function `maps` по образцу `scores`, модерация
  флагом вручную) — после плейтеста этапа A. Визуальный редактор и
  автоматическая модерация — не делать сейчас.
- **Кооп (§14)** — выбран вариант «две ракетки на одном поле» (общий пул жизней)
  как кандидат после этапа A карт; разделённое поле и «ракетки напротив» — не
  делать; онлайн на Supabase Realtime — вне горизонта (нет realtime-инфраструктуры,
  самый дорогой и рискованный пункт).
- Приоритеты ROADMAP переписаны: 1–4 (ядро) — реализованы, 5 — этап A карт,
  6 — локальный кооп, 7 — каталог карт.

**Производительность: превентивная оптимизация горячего пути** (сделано, 20ca49a):

- **Кэш градиентов** `render/gradCache.ts`: `gradient(ctx, key, make)` переиспользует
  CanvasGradient между кадрами (инвалидация по смене ctx; ключ отражает зависимости).
  Применён: блоки (tier-градиент один на tier + frozen; раньше до 150 create/кадр),
  фон (линейный по h; heat-градиент по wxh), виньетка (wxh), линия щита (по h).
  Меняющиеся во времени альфы вынесены в `globalAlpha` (heat, пульс щита).
- **Zero-alloc фильтрация** в игровом цикле: `utils.compactInPlace` (in-place
  компактация) вместо `.filter()` в `effects.step` (частицы/кольца/попапы/молнии —
  каждый кадр), шары в `updateStep`, блоки в `destruction` (2 места).
- **Zero-alloc очереди**: `utils.drainQueue` обрабатывает `boomQueue`/`sparkQueue`
  in-place, устойчив к цепным добавлениям во время обработки (взрывы бомб, звенья
  искр) — раньше два `.filter()` на кадр на каждую очередь.
- **Не сделано (осознанно, переоценить при §6-блоках):** пространственный хеш
  (блоков мало, проход дешёвый; станет актуально при дрейфующих/вращающихся),
  OffscreenCanvas-слои (мала выгода, риск с dpr и мок-тестами), пул объектов
  (краткожив��щие только частицы, уже ограничены MAX_PARTICLES=420).
- Тесты: `utils.test.ts` (+12) на clamp/rand/mulberry32/rotatedExtents и новые
  хелперы. **21 файл, 185 тестов — зелёные**; typecheck/lint/build — чисто.

**DEV-отладка в проде через VITE_DEBUG_TOOLS** (сделано, cb2c636):

- `input.ts`: F1 — FPS, F2 — хитбоксы, F3 — замедление, F4 — бессмертие
  (preventDefault); «+»/«-» на цифровой клавиатуре (зачистка/урон) тоже
  переведены на DEV-гейт — флаг `DEBUG_DAMAGE_KEY` удалён из `inputHost.ts`.
- Гейт — `DEBUG_TOOLS` (`src/config.ts`, `import.meta.env.VITE_DEBUG_TOOLS`
  !== "0"): включён во всех сборках, включая деплой beta; в стабильной версии
  (`main`) урезается передачей `VITE_DEBUG_TOOLS=0` в шаге build workflow.
- Новые поля `Game`: `showHitboxes`, `slowMotion`, `invincible`; `showFps`
  инициализируется только при включённой отладке.
- F3: `gameLoop` умножает dt на ×0.25 (FPS и hit-stop не искажаются).
- F4: `loseLife` при бессмертии не списывает жизнь — подаёт новый шар
  (попадание бомбы игнорируется).
- F2: `render/debug.ts` — `drawHitboxes` (эллипсы блоков с rot, круги шаров/
  боссов/бонусов/снарядов, прямоугольник ракетки с поворотом) + `drawDebugFlags`
  («DEV: SLOW ×0.25 · GOD» над FPS). Вызовы в `drawScene.ts`.
- Старая FPS-кнопка в углу меню **удалена** (вместе с дубль-стейтом
  `showFps` в `useDebugControls` и цепочкой props showFps/onToggleFps) —
  F1 единственный способ включить счётчик; состояние по-прежнему в localStorage.
- Сквозной тест keydown-цепочки: `debug.test.ts` перехватывает
  `window.addEventListener` фейкового окна, подключает `createInput` и дёргает
  F1–F4 (проверено: input → InputHost → debug-toggle → флаги).
- Тесты: `game/debug.test.ts` (5 тестов через `makeEnv`): F1–F4 по цепочке,
  toggle-флаги, бессмертие не списывает жизнь, контрольное списание,
  slowMotion ×0.25 в игровом цикле. **20 файлов, 173 теста — зелёные**;
  typecheck/lint/build — чисто.

**Мобильный HUD: неигровая зона при вертикальном экране — жёсткий ретеншн**
(сделано, 24.09.2026):

- `viewport.ts`: `HUD_TOP_PORTRAIT_CSS` 156 → 140 (HUD уплотнён — ниже).
- `hosts.ts`/`hostsWorld.ts`: `blockTop(g)` проброшен в `PowersWorld`/`PhysicsWorld`.
- `physics.ts`: верхняя стена шара теперь на `blockTop`, а не на y=0 — шар
  физически не может попасть в HUD-зону (в т.ч. после поворота экрана).
- `powers.ts`: периодический спавн блоков — только ниже `blockTop`;
  дрейф поля (`tryFieldShift`) больше не выталкивает блоки вверх (было
  `minY >= 6`, стало `minY >= blockTop`).
- `blockMotion.ts`/`updateStep.ts`: bob-дрейф медузы, вертикальный/круговой
  дрейф §6 и `fieldShift` клампятся нижней границей `blockTop + ry`.
- `ui/screens/hud.tsx`: портрет — компактные чипы (`px-2 py-1`, `text-base`,
  кнопки `h-8`), ряды `gap-1`, десктопные `sm:`-размеры в портрете отключены —
  HUD не растягивается даже на планшетах.
- Тесты: +8 (граница шара ×2, клампы дрейфа ×4, спавн/дрейф поля ×2) —
  **25 файлов, 222 теста — зелёные**.
- Нижняя панель эффектов (ШИРЕ/ОГНЬ/ЛАЗЕР…): на телефонах — горизонтальная
  полоса по центру нижнего края (`inset-x-1 bottom-2`), целиком ниже ракетки
  (тач-отступ 110 px оставляет снизу свободную полосу ~100 px); раньше
  `bottom-3 left-3 max-w-46vw` заворачивалась в колонку и налезала на ракетку.
  Чипы на телефонах компактнее (`px-1.5 py-0.5`, `sm:` возвращает прежние).
  Десктоп — без изменений (слева внизу).
- Подсказки «ТАП/КЛИК/ПРОБЕЛ — ЗАПУСК», «ЛАЗЕР ГОТОВ», «РАКЕТЫ»: на телефонах
  перенесены в полосу ниже ракетки (`bottom-14`, чипы компактнее), десктоп —
  как раньше (`bottom-[102px]`).
- Верхняя неигровая зона видна визуально: `render/background.ts#drawTopZone`
  рисует затемняющий градиент над `blockTop` + пунктирную cyan-линию отскока;
  вызов в `drawScene.ts` после частиц/попапов (градиент сходит к 0 у границы —
  шар при отскоке не затемняется).
- Босс: телепорт при «злой» фазе устранён — раньше `x = w/2 + sin(t·freq)` с
  фазозависимой частотой (0.6 → 1.1/1.5) давал скачок синуса в кадр перехода.
  Теперь фаза патруля интегрируется (`BossState.swayPhase/swayFreq`), частота
  плавно выходит на целевую (~0.5 с) — босс ускоряется на том же месте.
  `boss.test.ts` (новый): непрерывность x в кадр перехода + выход на частоту
  1.1; **26 файлов, 224 теста — зелёные**.
- `drawTopZone` показывается только в бою (`playing`/`paused`) **на вертикальном
  экране**; в ландшафте и вне уровня (меню/карта/итоги) не рисуется.
- **Сама зона тоже только в портрете**: `blockTop()` в `paddleControl.ts`
  возвращает 0 при `cssH <= cssW` — в ландшафте шар отскакивает от верхней
  кромки, спавн/дрейф блоков не клампятся сверху (механика откатилась к
  исходной через один хелпер — все потребители идут через `blockTop(g)`).
  `blockMotion.test.ts`: новые кейсы «ландшафт — зоны нет» / портретные лимиты;
  **27 файлов, 231 тест — зелёные**.
- Пересчёт при перевороте посреди уровня: `game/rotateLayout.ts` →
  `realignOnOrientationChange(g, wasPortrait)` из `resizeHandler` — ландшафт →
  портрет: блоки и босс (`baseY`) единым dy ниже новой зоны (расстановка не
  слипается), орбитальные/бомбовые блоки не трогаем (их каждый кадр считает
  босс); портрет → ландшафт и вне боя — без сдвига. Тест
  `rotateLayout.test.ts` (6 кейсов).
- Supabase: письмо об отмене автогрантов Data API (с 30.10.2025) отработано —
  боевая таблица не затронута (гранты сохраняются), но добавлена миграция
  `20250101000007_grants_api_access.sql` (select → anon/authenticated, all →
  service_role), чтобы не падали свежий проект / `supabase db reset` /
  preview-ветки и будущая Edge-работа; правило «GRANT в той же миграции, что и
  create table» записано в DEVELOPMENT.md, ROADMAP (§13 этап B) и techContext.
- Баг «невидимая медуза с полоской HP» (дуэт с рыбой) исправлен: оба существа
  строились вокруг `cx = w/2`, а второй `addMiniboss` вырезал `carveLevelBlocks`
  чужие блоки → купол медузы удалялся, группа без `dome` проваливалась в
  `drawFish` (пусто) → невидимка с HP-полоской. Фикс: `carveLevelBlocks` не
  трогает `isMiniboss`-блоки; `buildFish/buildJelly` приняли параметр `cx`,
  второе существо дуэта рождается со сдвигом `w*MINIBOSS_DUET_SHIFT` (0.22);
  рендер: группа без dome/body не рисуется вовсе. Тесты: 3 новых в
  `minibosses.test.ts` + интеграционный `minibossRuntime.test.ts` (2 кейса,
  28 файлов / 236 тестов — зелёные).
- Шкала HP минибоссов переработана: вместо `MINIBOSS_HP` (рыба 60 / медуза 50)
  с ростом ×(1+1.5) — линейный рост от `MINIBOSS_HP_FIRST = 20` на первом
  боевом ярусе до `MINIBOSS_HP_LAST = 100` на последнем боевом ярусе (28 из 30),
  округление до 5. Оба вида существ теперь в одном диапазоне. Вызов в
  `campaignFlow.ts` упрощён (`minibossHpFor(tier, tiers)`), дефолт `addMiniboss`
  — `MINIBOSS_HP_FIRST`.
- Новый финальный босс «ГРОЗОВАЯ МЕДУЗА» (`bossJelly.ts`, вариант
  `kind: "jellyfish"` в `bossVariants.ts`, открывается с 3-го яруса боссов):
  купол как у кракена (r=52) пульсирует по-настоящему — сжатие даёт рывок вверх
  (`jellyPulse`: scale 1→0.72, vy −110), расширение медленно опускает (vy +34);
  5 нижних отростков (4 сегмента, длина ≈ диаметр купола) привязаны к куполу —
  при сжатии разлёт уже, отростки вытянуты; тело неуязвимо, пока живы отростки.
  Раз в 10 с (в злой фазе 6 с) случайный отросток ~1.2 с искрит кончиком
  (телеграф) и выстреливает молнию-снаряд (950 px/с, летит в ракетку, движение
  как у бомб осьминога, `Block.bolt`; попадание — треск и голубые искры).
  Рендер медузы/молний/телеграфа — `render/boss.ts`; отладочный спавн —
  пункт «🪼 Медуза (босс)». Тесты: `bossJelly.test.ts` (пульс, отростки,
  интеграция цикла «искрение→выстрел», щит тела) + обновлены
  `bossVariants.test.ts`. 29 файлов / 244 теста — зелёные.
- Доработки медузы-босса по фидбеку: кончик отростка — старший живой сегмент
  (`tipBlock`/телеграф в рендере), поэтому после уничтожения прежних кончиков
  усечённые отростки продолжают стрелять; кольцо HP вокруг купола убрано;
  купол увеличен (r 52→68) и плавно сопряжён с отростками (линия щупальца
  стартует от точки крепления `jellyTentacleBase` на кромке, база отростка
  шире — 17 px, кромка купола углублена до 0.28r с бахромой до 0.62r);
  в меню отладки вместо эмодзи 🪼 (нет в некоторых шрифтах) используется
  готовая SVG-иконка `IconJelly` из `icons.tsx`. +1 регресс-тест (стрельба
  усечённых отростков). 29 файлов / 245 тестов — зелёные.
- Механика: `isPortrait()`/`hudTopCss()` в viewport, `blockTop()` = `hudTopCss()/scale`
  (клампы `h*0.14..h*0.35`), `ui/usePortrait.ts`; портрет — два ряда чипов,
  ландшафт — однострочный.

## Блокеры

Нет.

## Следующие шаги

1. Плейтест новых спецблоков (§6): баланс PULSE/SPRING/COTTON/PORTAL-констант,
   читаемость меток, поведение порталов рядом с щитом и на мобильных.
2. **Этап A пользовательских карт** (ROADMAP §13, решение от 24.09.2026) —
   старт после команды пользователя: формат «кода карты», импорт/экспорт,
   «мои карты», тест-прогон. До старта — ничего не реализовывать.
