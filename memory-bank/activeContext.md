# Активный контекст (Active Context)

> Обновлять в конце каждой сессии.

## Где мы сейчас (2026-09-22, поздний вечер)

Балансная правка дропов после рефакторинга:

- **Дроп с блоков реже**: шанс в `PowersSystem.dropPower` 0.24 → **0.12**
  (бонусы больше не спамят).
- **«Замедление» стало точечным спас-бонусом**: попадает в таблицу дропов
  только когда шар разогнан до **≥1.5× номинала** — т.е. при активном эффекте
  «ускорение» (×1.32) в сумме с перманентным разгоном зачистки (до ×1.24).
  Пока slow активен, условие ложно — дубли не набьются; при подборе slow
  отменяет fast.
- Новые чистые модули: `physics/speed.ts` (`ballSpeedMult`, `isBallSpedUp`,
  константы скорости — единая формула для физики и дропов) и
  `powers/table.ts` (`buildPowerTable` — взвешенная таблица с фильтрами
  campaign/boss/fewBlocks/spedUp). `physics.ts` использует `ballSpeedMult`,
  ре-экспорт через фасад.
- Тесты: `physics/speed.test.ts`, `powers/table.test.ts` — **17 файлов, 148
  тестов** зелёные; typecheck/lint чистые. Файлы: powers.ts 254, physics.ts 268.

Валидация пройдена; коммит и пуш в `beta` выполнены.

## Блокеры

Нет.

## Следующие шаги

- Проверить зелёный прогон CI после пуша (GitHub Actions → deploy).
- Дальнейший баланс/фичи — по `docs/ROADMAP.md`.
