# Активный контекст (Active Context)

> Обновлять в конце каждой сессии.

## Где мы сейчас (2026-09-23)

**Мобильный HUD: неигровая зона при вертикальном экране** (сделано):

- `viewport.ts`: `isPortrait(cssW, cssH)`, `hudTopCss()` — высота верхней
  неигровой зоны в CSS px: `HUD_TOP_CSS` = 96 (ландшафт),
  `HUD_TOP_PORTRAIT_CSS` = 156 (портрет, HUD двухрядный).
- `Game.cssW/cssH` заполняются в `resizeHandler`; `blockTop()` (paddleControl)
  считает зону блоков от `hudTopCss(g.cssW, g.cssH)/scale` — блоки, боссы
  и мини-боссы не заходят под плашки (пересчёт при следующем buildLevel).
- `ui/usePortrait.ts` — хук на `matchMedia("(orientation: portrait)")`;
  `App.tsx` прокидывает `portrait` в `HudOverlay`.
- `hud.tsx`: чипы вынесены в константы (scoreChip/goalsChip/bestChip/comboChip/
  coinsChip/shieldChip/rightControls); портрет — ряд 1 (счёт + цели | жизни +
  кнопки), ряд 2 (рекорд/серия/монеты/щит); ландшафт — прежняя однострочная
  раскладка с центральной плашкой целей на md+.
- Тесты: `viewport.test.ts` +3 (168 всего, зелёные); typecheck/lint/build —
  чисто. docs/DEVELOPMENT.md обновлён.

Новый бонус **«Электрошар»** (spark) — готов и в `beta` (5015dde): цепь искр
до 4 прыжков (радиус 95, задержка 0.07 с, `physics/spark.ts`), треск в полёте
(`physics/ballFx.ts`), молнии `render/fx.ts`, `SFX.zap()`, HUD-чип «ЭЛЕКТРО».
Попутно починен flaky `campaignMap.test.ts` (2 бага генератора
`campaignMapLayout.ts`: балансировка linkTiers + PAVA в assignYs).

## Блокеры

Нет.

## Следующие шаги

1. Плейтест портретного HUD на реальном телефоне (при необходимости подстроить
   `HUD_TOP_PORTRAIT_CSS` и раскладку рядов).
2. **Тема 1 — новые типы блоков (ROADMAP §6, приоритет 2):**

3. **Пульсирующий** — плавно циклично меняет размер (масштаб rx/ry по синусоиде,
   хитбокс пересчитывается каждый кадр; seeded-фаза из `seed` блока).
4. **Пружинный** — удар временно **ускоряет шар** (переиспользовать
   `physics/speed.ts`-механику локального таймерного множителя); отскок с
   пружинной анимацией сжатия-расжатия.
5. **Ватный** — удар временно **замедляет шар**; мягкая анимация, глухой звук.
6. **Крутящийся** — продолговатый блок: удар в одну из половин раскручивает его
   (`rotVel` от стороны и точки удара, гашение трением); угловая скорость меняет
   нормаль отскока последующих ударов.
7. **Дрейфующий** — траектории: горизонталь, вертикаль в пределах, по окружности;
   обобщить существующие `swayAmp`/`bobAmp` в маршрутизатор траекторий.
8. **Телепортирующий парный (чёрная дыра)** — переброс шара в парный блок с
   сохранением вектора скорости; кулдаун пары против зацикливания; виз/звук
   «воронки».

Технически: расширение `types.ts`/`levelBuilder`/`levelPatterns`, поведение —
чистыми модулями в `src/game/game/` + `src/game/physics/`, рендер —
`render/blocks.ts`; тесты рядом с каждым модулем.

**Тема 2 — обдумать (без реализации):**

- Целесообразность **пользовательских карт**: редактор, публикация, модерация
  администратором (первый этап — вручную), общая мод-модель → ROADMAP §13.
- **Кооперативный режим**: деление поля пополам / две ракетки на одном поле /
  ракетки напротив; приглашение игроков онлайн (Supabase Realtime) → ROADMAP §14.
- По итогам — решения «делать/не делать/когда» и приоритизация в ROADMAP.
