# Активный контекст (Active Context)

> Обновлять в конце каждой сессии.

## Где мы сейчас (2026-09-23)

Новый бонус **«Морозный мяч»** (frost):

- Подбор: `frostUntil = 8 c`, взаимоисключим с огнём (гасят друг друга); вес в
  таблице дропов — 8 (`powers/table.ts`).
- Удар морозного мяча по живому блоку **замораживает его и соседей в радиусе
  72** вместо урона (мяч отскакивает). Бомбы и блоки минибоссов не замораживаются.
- **Замороженный блок**: `frozen` + `hp=1` — колется с одного удара любым
  источником урона (мяч/лазер/ракета/взрыв), рассыпаясь **осколками-треугольниками**
  (`Particle.shape="shard"` с вращением) и звуком `SFX.iceShatter()`.
- **Рендер льда** (`render/blocks.ts: drawFrozenBlock`): ледяной градиент,
  2–3 процедурные трещины (детерминированные из `seed` через `mulberry32` —
  у каждого блока свои, не мерцают), блики «водяного льда» с дыханием по времени.
- Шар рисуется ледяным (`BallView.frost`); звук заморозки `SFX.freeze()`;
  HUD-чип «МОРОЗ», `frostOn` в HudData.
- Новые чистые модули: `physics/frost.ts` (`freezeCluster`) + тесты
  (`frost.test.ts`, 12 тестов). **18 файлов, 156 тестов** — зелёные;
  typecheck/lint/build — чистые. audio.ts 428 ≤500.

Валидация пройдена; коммит и пуш в `beta` выполнены.

## Блокеры

Нет.

## Следующие шаги

- Проверить зелёный прогон CI после пуша (GitHub Actions → deploy).
- Дальнейший баланс/фичи — по `docs/ROADMAP.md`.
