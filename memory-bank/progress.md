# Прогресс (Progress)

## Работает

- Движок: кампания с картой (фог-вар, события, минибоссы, боссы-осьминог/кракен),
  бесконечный режим, прокачка, достижения, эффекты, оружие (лазер/ракеты),
  мировой топ (Supabase + Edge Function), PWA, деплой CI.
- Спецблоки §6 (2026-09-23): пульсирующий, пружинный, ватный, крутящийся,
  дрейфующий, парный телепорт — `blockKinds`/`blockMotion`/`special`/`blockSpawn`
  - `render/blockKinds`; расстановка с 3-го уровня, детерминированная.
- Оптимизация горячего пути (2026-09-23): кэш градиентов (`render/gradCache`),
  zero-alloc фильтрация/очереди (`utils.compactInPlace`, `utils.drainQueue`).
- DEV-отладка F1–F4, двухстрочный портретный HUD.
- Все файлы `src/` ≤300 строк (`audio.ts` 448 ≤500 — исключение по правилам).
- Тесты: 24 файла, 214 тестов — зелёные; typecheck/lint/build — зелёные.

## История рефакторинга (обе волны — ветка `beta`, см. также `docs/REFACTORING.md`)

1. Волна 1: render/ (1388→модули), ui/screens/ (1409→модули), audio/ (1028→модули),
   physics/ (629→модули), App.tsx хуки, powers/apply, inputHost, campaignMapLayout,
   levelPatterns, тестовые хелперы.
2. Волна 2: game.ts 2366 → 299; свободные функции в `game/*` по хост-паттерну;
   тип `MinibossCreature` в types.ts; `MinibossCreature`-поле `Game.minibosses`.

## Что дальше

- Спецблоки §6 реализованы; дальше плейтест баланса констант.
- Тема 2 продумана (24.09.2026): карты — делать поэтапно (этап A «коды карт»
  следующий), кооп — локальный «две ракетки» после карт, онлайн — не сейчас.
- Далее: этап A карт по `docs/ROADMAP.md` §13 после подтверждения.
- Известных блокеров нет.
